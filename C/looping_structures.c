#include <stdio.h>

int main() {
    for (int i = 0; i < 5; i++) {
        printf("i = %d\n", i);
    }

    int j = 0;
    while (j < 5) {
        printf("j = %d\n", j);
        j++;
    }

    return 0;
}
